#!/bin/sh
# Load user environment to find pnpm and node
if [ -s "$HOME/.bashrc" ]; then
  . "$HOME/.bashrc" 2>/dev/null
fi

# NVM must come after bashrc, as bashrc might initialize it
if [ -s "$HOME/.nvm/nvm.sh" ]; then
  . "$HOME/.nvm/nvm.sh"
fi

# Run lint-staged
pnpm exec lint-staged
